name: Image

# On push to main: build + push the neuro-ms GPU stack image to the private GHCR.
# On pull_request: build only (validates the Dockerfile) — no push.
# FROM ghcr.io/medmcp/base — the base package must grant this repo read access
# (Package settings -> Manage Actions access), and base:main must already be
# published (run medmcp-dev's Images workflow first). amd64 only for now (torch
# cu128 for HD-BET + onnxruntime-gpu + baked LST-AI models / HD-BET weights;
# ~13 GB, so no layer caching — see the build step).
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      # The neuro-ms image is large (CUDA torch + onnxruntime-gpu + baked weights);
      # reclaim space the hosted runner pre-populates so the build doesn't run out.
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/lib/android "${AGENT_TOOLSDIRECTORY:-/opt/hostedtoolcache}" || true
          df -h /
      - uses: docker/setup-buildx-action@v4
      # Login on every event — needed to pull the private base, even for PR builds.
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          build-args: |
            BASE_IMAGE=ghcr.io/medmcp/base:main
          tags: |
            ghcr.io/medmcp/neuro-ms:${{ github.sha }}
            ghcr.io/medmcp/neuro-ms:main
          # No GHA cache: this image is ~13 GB, over the ~10 GB Actions-cache cap, and
          # `cache-to: mode=max` exporting a second copy of every layer fills the runner
          # disk. The build fits on the hosted runner (after the Free-disk step), so we
          # just skip caching.
