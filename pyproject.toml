[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[project]
name = "medmcp-neuro-ms"
version = "0.1.0"
description = "MS white-matter lesion segmentation (LST-AI) for the medmcp ecosystem."
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "Julian McGinnis", email = "julian.mcginnis@tum.de" },
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Healthcare Industry",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Image Processing",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
]
keywords = ["medical", "image processing", "ai-agent", "mcp", "multiple sclerosis", "lesion segmentation", "lst-ai"]
requires-python = ">=3.12"
# The wrapper venv stays light: just MCP. LST-AI (from PyPI: native-PyTorch UNet3D
# ensemble + picsl-greedy + HD-BET) and its torch/CUDA stack are installed into an
# isolated venv (/opt/lst-venv) by the Dockerfile and invoked as the `lst` subprocess —
# see _neuro_ms.py.
dependencies = [
    "mcp>=1.0",
]

[project.scripts]
medmcp-neuro-ms = "medmcp_neuro_ms.server:main"

[project.entry-points."medmcp.stacks"]
medmcp-neuro-ms = "medmcp_neuro_ms.server:server_config"

# How a downstream "core" package declares this package as an optional extra:
# [project.optional-dependencies]
# neuro = ["medmcp-neuro>=0.1"]

[project.urls]
Homepage = "https://github.com/medmcp/medmcp-neuro-ms"
Issues = "https://github.com/medmcp/medmcp-neuro-ms/issues"

# Pin git-sourced medmcp packages during development:
# [tool.uv.sources]
# medmcp-dicom = { git = "https://github.com/medmcp/medmcp-dicom", branch = "main" }

[dependency-groups]
test = [
    "pytest>=9",
    "pytest-asyncio>=1.2",
]
quality = [
    "ruff>=0.15",
    "pyright>=1.1.400",
    "pre-commit>=4.5",
]
dev = [
    { include-group = "test" },
    { include-group = "quality" },
]

[tool.uv]
default-groups = ["dev"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E", "W",   # pycodestyle
    "F",        # pyflakes
    "I",        # isort
    "N",        # pep8-naming
    "UP",       # pyupgrade
    "B",        # flake8-bugbear
    "SIM",      # flake8-simplify
    "RUF",      # ruff-specific
    "D",        # pydocstyle
]

ignore = ["D100", "D104"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.format]
quote-style = "double"

[tool.pyright]
include = ["src", "tests"]
pythonVersion = "3.12"
typeCheckingMode = "strict"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
